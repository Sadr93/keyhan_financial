# راهنمای اجرای سرور محلی

## روش 1: استفاده از Python (پیشنهادی)

### در Terminal:

```bash
cd /Users/mohammadsadr/Desktop/Sadrun/keyhan_financial
python3 -m http.server 8000
```

### سپس در مرورگر:
```
http://localhost:8000
```

یا

```
http://127.0.0.1:8000
```

---

## روش 2: استفاده از Node.js

اگر Python ندارید:

```bash
cd /Users/mohammadsadr/Desktop/Sadrun/keyhan_financial
npx http-server -p 8000
```

---

## روش 3: استفاده از VS Code Live Server

1. VS Code را باز کنید
2. افزونه "Live Server" را نصب کنید
3. روی فایل `index.html` راست کلیک کنید
4. "Open with Live Server" را انتخاب کنید

---

## مشکلات رایج

### مشکل: "Port 8000 is already in use"
**راه حل:** از پورت دیگری استفاده کنید:

```bash
python3 -m http.server 8080
```

سپس به `http://localhost:8080` بروید

---

### مشکل: "python3: command not found"
**راه حل:** از `python` به جای `python3` استفاده کنید:

```bash
python -m http.server 8000
```

---

### مشکل: صفحه سفید یا خطا می‌بیند
**بررسی کنید:**
1. آیا در مسیر درست هستید؟ (`pwd` را بزنید)
2. آیا فایل `index.html` وجود دارد؟
3. Console مرورگر را باز کنید (F12) و خطاها را ببینید

---

### مشکل: "Cannot GET /"
**راه حل:** مطمئن شوید که `index.html` در همان پوشه‌ای است که سرور را اجرا می‌کنید

---

## نکات مهم

- ✅ سرور باید در **همان پوشه** که فایل‌های پروژه هستند اجرا شود
- ✅ Terminal را **باز نگه دارید** - اگر ببندید سرور متوقف می‌شود
- ✅ برای توقف سرور: `Ctrl + C` را در Terminal بزنید
- ✅ بعد از تغییر فایل‌ها، فقط صفحه را Refresh کنید (F5)

---

## تست سریع

بعد از اجرای سرور، این آدرس را در مرورگر باز کنید:

```
http://localhost:8000/index.html
```

یا فقط:

```
http://localhost:8000
```

اگر صفحه باز شد، یعنی همه چیز درست است! 🎉

